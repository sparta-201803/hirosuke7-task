<h1>タスク一覧</h1>
<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %>
  <%= link_to "変更", task_path(task) %>|
  <%= link_to "削除", task_path(task), method: :delete %></<li>
  <% end %>
</ul>
<hr>
<%= render partial: "new_task", locals: { message: @task } %>
